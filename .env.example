PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pixelmedia
CORS_ORIGIN=http://localhost:5173
AUTO_MIGRATE=true
NODE_ENV=development
REQUIRE_DATABASE=false

# Local fallback is disabled in production unless explicitly enabled.
VITE_ENABLE_LOCAL_FALLBACK=false

# Optional. Keep model keys only on the backend.
OPENAI_API_KEY=
GEMINI_API_KEY=
